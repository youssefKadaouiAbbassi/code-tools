# Project-scope Claude Code rules

The global rules live in `~/.claude/CLAUDE.md` — all of research-first MCP use, skill-before-code, coding style, and proactive follow-up apply here too.

This file layers **project-specific** context on top. Add entries as the project's stack, conventions, or constraints emerge. Keep it short — everything in the global file is already in effect.

## Stack

<!-- Fill in as the project takes shape. Examples:
- Language: TypeScript + Bun
- Tests: `bun test tests/`
- Lint: `bun run lint`
- Entry: `bin/<name>.ts`
-->

## Project conventions

<!-- Only list deltas from the global style. Examples:
- PR titles use Conventional Commits (`feat:`, `fix:`, `refactor:`)
- Migrations live in `db/migrations/` and run in numeric order
-->

## Runtime requirements

<!-- Things the user needs running locally. Examples:
- Postgres on localhost:5432
- `bun install` before first run
-->

---
*Symlinked to `AGENTS.md` and `GEMINI.md` for cross-tool portability.*
