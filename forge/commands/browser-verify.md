---
description: UI parcel verification via bundled headless Playwright + ProofShot bundle.
argument-hint: parcel id
---

Parcel id: $ARGUMENTS

Run the browser-verify runbook in `skills/browser-verify/SKILL.md`. Drive bundled Chromium headless (NOT host browser). Produce `.forge/browser/<parcel>.proofshot` + verdict JSON.
