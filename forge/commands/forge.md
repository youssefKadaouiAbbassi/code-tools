---
description: One-command coding pipeline — plan → research → council → code → verify (PBT + mutation + browser) → ship PR with signed audit chain.
argument-hint: brief describing the feature to ship
---

Brief: $ARGUMENTS

Use the Task tool to spawn forge-lead with this brief. forge-lead reads `skills/forge/SKILL.md` for the runbook.
