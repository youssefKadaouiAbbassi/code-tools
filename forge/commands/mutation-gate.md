---
description: Per-stack mutation testing — Stryker / mutmut / cargo-mutants — gate ≥ 0.80.
argument-hint: parcel id
---

Parcel id: $ARGUMENTS

Run the mutation-gate runbook in `skills/mutation-gate/SKILL.md`. Produce `.forge/mutation/<parcel>.json`.
