---
description: Property-based testing gate — fast-check / Hypothesis / proptest — emit VERIFIED / PARTIAL / MISSING.
argument-hint: parcel id
---

Parcel id: $ARGUMENTS

Run the pbt-verify runbook in `skills/pbt-verify/SKILL.md`. Delegate generation to the pbt-verifier subagent. Produce `.forge/pbt/<parcel>.json`.
